log的输出太频繁，不用每个epoch输出一次，保存成tensorboard记录曲线就够了？

logfile建议按方法创建新的子文件夹

有些重复的参数是不是可以删去，避免每次调整都得做很多重复的修改工作
比如task_num? 以及init_cls_num会在classifier中重复有一个参数

trainer256行评估的时候建议加一个并列条件 or (epoch_idx+1)==self.inc_epoch，不仅过程中评估，训练结束肯定要评估的，这样调代码的时候就不用
对val_per_epoch进行可能的调整了

写了一个获得均值方差的函数